{
    "name": "spbs-root",
    "version": "1.0.0",
    "scripts": {
        "start": "npm run start --prefix backend & npm run dev --prefix frontend",
        "dev": "concurrently \"npm run dev --prefix frontend\" \"nodemon --prefix backend server.js\"",
        "install-all": "npm install --prefix frontend && npm install --prefix backend"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}